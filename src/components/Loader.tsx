import { ProgressBar } from 'react-loader-spinner'

const Loader = () => {
	return <ProgressBar visible={true} borderColor='#213131' width='100%' wrapperClass='text-center' />
}

export default Loader
